apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: istio-system

resources:
  - namespace.yaml
  - istio.yaml
  - ingress-class.yaml
  - petclinic-gateway.yaml
  - petclinic-certificate.yaml
  - ingressgateway-request-auth.yaml
  - ingressgateway-auth-policy.yaml

patchesStrategicMerge:
  - patches/delete-egressgateway.yaml