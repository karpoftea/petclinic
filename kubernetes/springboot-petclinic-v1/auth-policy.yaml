apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: springboot-petclinic
spec:
  selector:
    matchLabels:
      app: springboot-petclinic-v1
  action: ALLOW
  rules:
    - to:
        - operation:
            paths: [ "/.well-known/*" ]
    - to:
        - operation:
            paths: [ "/vets" ]
      from:
        - source:
            requestPrincipals: [ "*" ]
      when:
        - key: request.auth.claims[https://petclinic.ikarpov.net/roles]
          values: [ "petclinic-admin" ]
    - to:
        - operation:
            notPaths: [ "/vets", "/.well-known/*" ]
      from:
        - source:
            requestPrincipals: [ "*" ]
      when:
        - key: request.auth.claims[https://petclinic.ikarpov.net/roles]
          values: [ "petclinic-manager", "petclinic-admin" ]

##### some tips and trics here:
#    - to:
#        - operation:
#            methods: [ "GET" ]
#            paths: [ "/", "*.css", "*.js", "*.html", "*.png", "*.woff", "*.woff2", "*.ico", "/manage/health", "/.well-known/*" ]



#apiVersion: security.istio.io/v1beta1
#kind: AuthorizationPolicy
#metadata:
#  name: springboot-petclinic
#spec:
#  selector:
#    matchLabels:
#      app: springboot-petclinic-v1
#  action: ALLOW
#  rules:
#    - from:
#        - source:
#            requestPrincipals: [ "*" ]



#apiVersion: security.istio.io/v1beta1
#kind: AuthorizationPolicy
#metadata:
#  name: allow-all
#spec:
#  action: ALLOW
#  # This matches everything.
#  rules:
#    - {}